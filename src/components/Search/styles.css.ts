import { style } from '@vanilla-extract/css';

export const searchStyle = style({
    margin: '10px',
    padding: '10px',
    alignItems: 'center',
});


export const inputStyle = style({
    maxWidth: '90vw',
    width: '200px',
    fontSize: '14px',
});
